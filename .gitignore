# Rust
/target
**/target
Cargo.lock

# Node
node_modules
.pnpm-store
dist
build

# Tauri
apps/zylcode-desktop/src-tauri/target

# Vite
apps/zylcode-desktop/dist

# TypeScript
*.tsbuildinfo

# Env
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/settings.json
.idea/
*.swp
*.swo
*~

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*

# Local SQLite caches
*.db
vector_cache.db
vector_cache.db.bak
zylcode_telemetry.db

deepseek-*.html
deepseek-repos.json
search_openai.py

# Agent workspace (local memory + session state — never published)
.workbuddy-ai/

# Python (CI guard scripts)
__pycache__/
*.pyc

# Parallel-execution worktrees (isolated agent working trees)
# Must be inside the project for the sandbox to permit cargo writes.
# See PARALLEL_EXECUTION_MANIFEST.md §3.
.wt/

# Agent tooling state (vision attachment scratch space, client session state)
.dsh-vision-toolkit/
.freebuff/

# ZylCode per-project session state (agent memory, evidence, recovery markers)
.zylcode/
